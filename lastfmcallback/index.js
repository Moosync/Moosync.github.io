if (window.location.pathname.includes('lastfmCallback')) {
    open('com.moosync://lastfmCallback' + window.location.search)
}

function test () {
    open('com.moosync://lastfmCallback' + window.location.search);
}